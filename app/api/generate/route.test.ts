describe("Generate Route", () => {
  describe("When the recipe generation is successful", () => {
    it("Should return an array of recipes for each recipe", () => {
      expect(1 + 1).toBe(2);
    });
  });
  
  
  describe("When the recipe generation fails", () => {
    it("Should return an array of recipes for each recipe", () => {
      expect(1 + 1).toBe(2);
    });
  });
});